# Strategy logic version registry (K75 pattern — see Cemini wiki pnl-attribution-versioning)
# Bump current_version_id + deployed_at on every material signal/sizing/execution change.

strategy_key: pm_wc_advance_lp

current:
  version_id: wc_advance_lp_v2
  deployed_at: "2026-05-30T12:00:00Z"
  note: "Fill handler module 4 — 60s limit exit, queue depletion, kill switch"

legacy_version_ids:
  - wc_advance_lp_v1
  - legacy_unversioned
