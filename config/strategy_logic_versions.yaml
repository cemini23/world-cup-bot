# Strategy logic version registry — bump on material logic/sizing/execution changes.
# Bump current_version_id + deployed_at on every material signal/sizing/execution change.

strategy_key: pm_wc_advance_lp

current:
  version_id: wc_advance_lp_v6
  deployed_at: "2026-06-09T12:00:00Z"
  note: "Watch hydrate from ledger; reconcile cursor fix; async fill path; quote batch failures surfaced"

legacy_version_ids:
  - wc_advance_lp_v5
  - wc_advance_lp_v4
  - wc_advance_lp_v3
  - wc_advance_lp_v2
  - wc_advance_lp_v1
  - legacy_unversioned
